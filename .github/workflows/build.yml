name: Build
permissions:
    contents: read
on:
    pull_request:
    push:
        branches:
            - main
jobs:
    build-and-test:
        uses: dnd-mapp/actions/.github/workflows/build.yml@main
